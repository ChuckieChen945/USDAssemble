#usda 1.0
(
    defaultPrim = "${component_name}"
    metersPerUnit = 1
    upAxis = "Y"
)

over "${component_name}"
{
    def Scope "Geom" (
        prepend references = @./${component_name}_geom.usd@
    )
    {
    }
}
