#usda 1.0
(
    defaultPrim = "${assembly_name}"
    doc = "example"
    metersPerUnit = 1
    upAxis = "Y"
)

class "__class__"
{
    class "${assembly_name}"
    {
    }
}

def Xform "${assembly_name}" (
    kind = "assembly"
    prepend inherits = </__class__/${assembly_name}>
)
{

}
