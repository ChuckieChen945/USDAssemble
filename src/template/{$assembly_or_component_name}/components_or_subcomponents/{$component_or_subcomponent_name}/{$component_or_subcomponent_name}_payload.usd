#usda 1.0
(
    defaultPrim = "${component_or_subcomponent_name}"
    metersPerUnit = 1
    subLayers = [
        @./${component_or_subcomponent_name}_look.usd@,
        @./${component_or_subcomponent_name}_geom.usd@
    ]
    upAxis = "Z"
)

